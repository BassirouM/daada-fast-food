import * as React from "react";
import { cn } from "../utils/cn";

interface PremiumGateProps {
  onUnlock?: () => void;
  className?: string;
  children?: React.ReactNode;
}

export function PremiumGate({ onUnlock, className, children }: PremiumGateProps) {
  return (
    <div className={cn("relative overflow-hidden rounded-2xl", className)}>
      {/* Blurred content */}
      {children && (
        <div className="pointer-events-none select-none blur-md">{children}</div>
      )}
      {/* Overlay */}
      <div className="absolute inset-0 flex flex-col items-center justify-center gap-4 bg-black/20 backdrop-blur-sm">
        <div className="flex flex-col items-center gap-3 rounded-2xl bg-white/95 p-6 shadow-xl dark:bg-gray-900/95">
          <div className="flex h-12 w-12 items-center justify-center rounded-full bg-gradient-to-br from-amber-400 to-yellow-500 text-2xl">
            ✨
          </div>
          <div className="text-center">
            <h3 className="font-display text-lg font-bold text-gray-900 dark:text-white">
              Contenu Premium
            </h3>
            <p className="mt-1 text-sm text-gray-500 dark:text-gray-400">
              Débloquez cette recette et 50+ autres
            </p>
          </div>
          <button
            onClick={onUnlock}
            className="w-full rounded-xl bg-gradient-to-r from-amber-500 to-yellow-400 py-2.5 text-sm font-semibold text-white shadow-sm hover:opacity-90 transition-opacity"
          >
            Débloquer — 1 500 FCFA/mois
          </button>
        </div>
      </div>
    </div>
  );
}
